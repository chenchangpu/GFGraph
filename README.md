# GFGraph

## Dependency
* Linux (Ubuntu)
* C++ (GCC) >= 9.4.0
* CMake     >= 3.12
* OpenMP    >= 4.5
* conda     >= 22.9.0
* python    >= 3.10

## Build
```sh
mkdir build && cd build
cmake ..
make
```